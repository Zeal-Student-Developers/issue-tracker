const authController = require("./authController");
const userController = require("./userController");
const issueController = require("./IssueController");

module.exports = { authController, userController, issueController };
