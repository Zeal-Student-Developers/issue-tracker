const authRoutes = require("./auth");
const userRoutes = require("./user");
const issueRoutes = require("./issue");
const statsRoutes = require("./stats");

module.exports = { authRoutes, userRoutes, issueRoutes, statsRoutes };
